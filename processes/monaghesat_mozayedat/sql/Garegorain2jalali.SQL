-- convert
UPDATE prc_db_mozayedat_monaghesat
SET DEADLINE_ASNAD = 
    CASE 
        WHEN DEADLINE_ASNAD = '0000-00-00 00:00:00' THEN DEADLINE_ASNAD
        WHEN DEADLINE_ASNAD LIKE '14%' THEN DEADLINE_ASNAD
        ELSE n2_date(DEADLINE_ASNAD)
    END
WHERE DEADLINE_ASNAD IS NOT NULL;